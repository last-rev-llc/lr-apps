---
issue: 21
status: success
test_fix_retries: 0
duration: 245
date: 2026-04-08
---
## What Worked
- Standard auth gate pattern applied cleanly: layout async + `requireAppLayoutAccess`, registry `auth: true`, and smoke tests all followed the established template from prior PRs (#100-#104)
- Zero test retries — the repeatable test structure (mock `requireAppLayoutAccess`, assert redirect errors) continues to be reliable across all auth gate issues
- Agent also backfilled auth gates and tests for several other ungated apps (age-of-apes, alpha-wins, area-52) in the same run, plus improved shared auth UX (login form redirect propagation, unauthorized page copy)

## What Failed
- Nothing — all 109 web tests passed on first run, full turbo cache hit across all 8 packages

## Patterns
- **Batch auth gate PRs share a diff shape**: layout becomes async, adds one `requireAppLayoutAccess` call, test file is a near-identical 4-test suite. This is a strong candidate for a codegen template or skill
- **Shared UX improvements piggyback on feature work**: login redirect slug passthrough and unauthorized page copy tweaks were bundled into the auth gate PR without scope creep

## Anti-Patterns
- **Scope expansion without issue tracking**: the run touched 6+ apps beyond soccer-training (age-of-apes, alpha-wins, area-52, etc.) and modified shared auth pages — these changes should ideally map to their own issues for traceability
- **Test files use `globalThis.React = React` hack**: this works but is brittle; a shared test setup in `@repo/test-utils` would be cleaner

## Suggested Skill Updates
- None
