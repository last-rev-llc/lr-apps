---
issue: 230
status: success
test_fix_retries: 0
duration: 435
date: 2026-04-21
retries: 0
traces:
  plan: .alpha-loop/sessions/session/017-cleanup-backlog/traces/prompts/plan-issue-230.md
  implement: .alpha-loop/sessions/session/017-cleanup-backlog/logs/issue-230-implement.log
  review: .alpha-loop/sessions/session/017-cleanup-backlog/traces/outputs/review-issue-230.log
  diff: .alpha-loop/sessions/session/017-cleanup-backlog/diffs/issue-230.diff
---

## What Worked
- Used HTML comment markers (`<!-- lib-listing:start -->` / `<!-- lib-listing:end -->`) to delimit the canonical listing in CLAUDE.md, giving the sync script an unambiguous parse target.
- Added script to `pnpm lint` pipeline so enforcement runs in CI without needing a separate workflow step.
- All 810 web tests and 4 new script tests passed on first run (0 retries); lint problem count dropped from 284 to 282.

## What Failed
- Nothing — all acceptance criteria met on first attempt.

## Patterns
- Doc-vs-filesystem sync checks benefit from explicit markers in the doc; regex-scraping a "lib/" heading section is brittle when nearby prose also mentions `lib/`.
- Bundle the doc update and the check enablement in the same PR so CI doesn't fail immediately on merge.
- Non-recursive directory listing (top-level only) keeps the canonical list stable against deep subdirectory churn.

## Anti-Patterns
- Scope creep: the PR also touched `apps/web/app/apps/age-of-apes/` (Badge/Button/Card/Input migration + new tests) which is unrelated to issue #230's CI-check scope — belongs in a separate PR.
- Leaving three info-severity findings from a deleted `review-issue-68.json` unaddressed without a tracking issue risks losing them.

## Suggested Skill Updates
- None
