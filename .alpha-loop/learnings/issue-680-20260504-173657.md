---
issue: 680
status: failure
test_fix_retries: 0
duration: 1171
date: 2026-05-05
retries: 0
traces:
  plan: .alpha-loop/sessions/session/052-ai-eval-shared-package-foundation/traces/prompts/plan-issue-680.md
  implement: .alpha-loop/sessions/session/052-ai-eval-shared-package-foundation/logs/issue-680-implement.log
  review: .alpha-loop/sessions/session/052-ai-eval-shared-package-foundation/traces/outputs/review-issue-680.log
  diff: .alpha-loop/sessions/session/052-ai-eval-shared-package-foundation/diffs/issue-680.diff
---

## What Worked
- All five migrations (#677-#681) implemented and paired correctly; `pnpm db:check-migration-pairs` passes across 33 migrations
- Storage bucket #680 acceptance criteria fully met: idempotent inserts, asymmetric policies (no INSERT on chatflow-results), 25MB/50MB caps with correct mime restrictions
- PGSODIUM_KEY_ID wired end-to-end (env schema with UUID validation, turbo.json globalEnv, .env.example, all 3 CI workflows); 18 env tests pass

## What Failed
- 6 proxy integration tests in `apps/web/__tests__/proxy.integration.test.ts` failed on `auth.apps.lastrev.com` host parsing — rewrites returning null where truthy expected; unrelated to migration work in scope
- Orphaned test file `scripts/__tests__/seed-pgsodium-key.test.ts` never runs in CI (scripts/ not a workspace package, no vitest install)

## Patterns
- Idempotent pgsodium key seeding via stable name lookup (`chatflow_targets_api_token_v1`) before create — supports dry-run and re-runs cleanly
- Storage bucket migration pattern: `on conflict (id) do nothing` for idempotent bucket inserts + named per-policy CREATE POLICY statements scoped via `storage.foldername(name)[1] = auth.uid()::text`
- Asymmetric RLS for service-role-only writes: omit INSERT policy entirely (default-deny) rather than adding a restrictive one

## Anti-Patterns
- Adding test files outside any workspace package — they silently never execute, giving false confidence
- Bundling unrelated migration work into one session without verifying the unaffected test suite passes first (proxy test regression went unnoticed and unfixed)

## Suggested Skill Updates
- `testing-patterns`: add a check that new `*.test.ts` files live under a workspace package listed in `pnpm-workspace.yaml`, or are wired into a root script that invokes vitest
- `git-workflow` / pre-merge: when a session bundles multiple issues, run the full test suite once before declaring success — pre-existing regressions outside the changed surface still block merge
